workspace: ..
campaign_dir: ../research_state/campaign-epsilon-2-10-ultra

campaign:
  operating_objective: maximize_verified_dimension
  researcher_count: 40
  verifier_count: 1
  roadmap_count: 3
  literature_agent_enabled: true
  lemma_writer_enabled: true
  genius_enabled: false
  executable: codex
  model: gpt-5.6-sol
  reasoning_effort: ultra
  verifier_reasoning_effort: xhigh
  researcher_reasoning_efforts: [ultra, max, xhigh, max, ultra, xhigh, max, ultra, xhigh, max]
  synthesis_reasoning_effort: xhigh
  require_polynomial_generator_matrix: true
  disable_nested_agents: true
  max_workers: 10
  timeout_seconds: 5400
  max_attempts: 3
  # Wrap-up mode: exhaust the already-durable queue once, then stop. Do not
  # generate another research wave if no verified improvement is found.
  max_improvement_rounds: 1
  retry_seconds: 120
  auto_sync_git: true
  git_remote: origin
  git_branch: main
  block_length: 1073741824
  epsilon_numerator: 1
  epsilon_denominator: 1024
  relative_distance_numerator: 511
  relative_distance_denominator: 1024
  minimum_distance: 535822336
  current_record_dimension: 1040
